// config.example.js - Example Stripe Payment Links
// This file is safe to commit to GitHub.
// Copy this file to config.js and fill in your actual Stripe links.

const config = {
  // Webhook URL for n8n
  WEBHOOK_URL: 'YOUR_N8N_WEBHOOK_URL',

  // Max image size in MB
  MAX_IMAGE_SIZE_MB: 15,

  // Allowed image types (MIME types)
  ALLOWED_IMAGE_TYPES: 'image/jpeg,image/png,image/heic,image/heif'
};
